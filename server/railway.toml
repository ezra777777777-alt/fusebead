[build]
  builder = "nixpacks"
  buildCommand = "npm install && npm run build"

[deploy]
  startCommand = "npm run start"

# For SQLite persistence, add a volume in Railway dashboard:
# Mount path: /app/data
# This maps to server/data/ where fusebead.db lives
